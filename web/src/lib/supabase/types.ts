/**
 * Placeholder — regenerate once the Supabase project is authenticated:
 *
 *   npx supabase login
 *   npx supabase link --project-ref <ref>
 *   npx supabase gen types typescript --linked > src/lib/supabase/types.ts
 *
 * Until then this is intentionally untyped so the app compiles; the actual
 * schema contract lives in supabase/migrations/.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
